<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RegistroUsuario" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/rojo</item>
    </style>

    <style name="Theme.RegistroUsuario" parent="Base.Theme.RegistroUsuario" />
    <style name="estilo_botones" parent="Widget.Material3.Button">
        <item name="android:background">@drawable/btn_style</item>
        <item name="backgroundTint">@color/rojo</item>
    </style>
    <style name="btn_eliminar" parent="Widget.Material3.Button">
        <item name="android:background">@drawable/btn_eliminar</item>
        <item name="backgroundTint">@color/gris</item>
    </style>
</resources>